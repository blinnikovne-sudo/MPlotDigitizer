# Альтернативные методы сборки APK

Это руководство описывает альтернативные подходы к сборке Android APK когда стандартная локальная сборка невозможна.

## Проблема

Для сборки Android APK **всегда требуется**:
1. Android SDK Build Tools (aapt2, d8, zipalign, apksigner)
2. Android Platform SDK (android.jar)
3. Интернет для загрузки Maven зависимостей (при первой сборке)

**Альтернатив Gradle не существует** - все современные инструменты (Capacitor, Cordova, React Native) используют Gradle под капотом.

## ✅ Решение 1: Docker с предустановленным Android SDK (Рекомендуется)

### Преимущества
- Все зависимости предустановлены в образе
- Не требует установки Android SDK на хост-систему
- Воспроизводимая среда сборки
- Работает на любой ОС с Docker

### Как использовать

```bash
# Вариант A: Через скрипт
./build-apk-docker.sh

# Вариант B: Через docker-compose
docker-compose -f docker-compose.android.yml up --build

# Вариант C: Вручную
docker build -f Dockerfile.android -t wpd-android .
docker run --rm -v "$(pwd)/apk-releases:/app/apk-releases" wpd-android
```

### Первый запуск
При первом запуске Docker скачает (~600 MB):
- Ubuntu базовый образ
- Android SDK командные инструменты
- Android Build Tools
- Android Platform SDK
- Gradle

**Требуется интернет только при первой сборке образа!**

После этого образ можно сохранить и использовать офлайн:
```bash
# Сохранить образ
docker save wpd-android > wpd-android.tar

# Загрузить на другой машине без интернета
docker load < wpd-android.tar
```

---

## ✅ Решение 2: GitHub Actions (CI/CD) ⭐ УЖЕ НАСТРОЕНО

### Преимущества
- Полностью автоматическая сборка
- Бесплатно для публичных репозиториев
- Не требует локальной настройки
- **✅ УЖЕ РАБОТАЕТ** - просто сделайте push!

### ⚡ Быстрый старт

**Автоматическая сборка:**
```bash
git push  # APK соберется автоматически на GitHub!
```

**Ручной запуск:**
1. Откройте GitHub → Actions
2. Выберите "Build Android APK"
3. Нажмите "Run workflow"

**Получить APK:**
1. GitHub → Actions → выберите завершенный run
2. Скачайте из "Artifacts"
3. Или сделайте `git pull` - APK будет в `apk-releases/debug/`

### Подробности

**Настроено 2 workflow:**
1. `.github/workflows/build-android.yml` - автоматическая сборка debug APK
2. `.github/workflows/release-apk.yml` - сборка release APK

**Подробная документация:** [.github/workflows/README.md](.github/workflows/README.md)

**Что происходит:**
- При каждом push GitHub автоматически собирает APK
- APK загружается как Artifact (доступен 30 дней)
- APK автоматически коммитится в `apk-releases/debug/`
- Время сборки: ~5-10 минут

**Никакой настройки не требуется** - просто делайте push!

---

## ✅ Решение 3: Онлайн сборка (App Center / Codemagic)

### Microsoft App Center
```bash
# Установка CLI
npm install -g appcenter-cli

# Настройка
appcenter login
appcenter apps create -d WebPlotDigitizer -o Android -p React-Native

# Сборка
appcenter build queue
```

### Codemagic
1. Подключите GitHub репозиторий
2. Выберите Android в настройках
3. Codemagic автоматически соберет APK

---

## ✅ Решение 4: Готовый Docker образ

Используйте готовый образ с Android SDK:

```bash
# Использование готового образа mingc/android-build-box
docker run --rm \
  -v "$(pwd):/project" \
  -w /project \
  mingc/android-build-box:latest \
  bash -c "npm install && ./build-apk.sh"
```

---

## ✅ Решение 5: Progressive Web App (PWA) вместо APK

### Преимущества
- Не требует сборки APK
- Работает на всех платформах
- Устанавливается через браузер
- Автоматические обновления

### Настройка PWA

1. Создайте `manifest.json`:
```json
{
  "name": "WebPlotDigitizer",
  "short_name": "WPD",
  "start_url": "/index.html",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#2196f3",
  "icons": [
    {
      "src": "/images/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/images/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

2. Создайте Service Worker `sw.js`:
```javascript
const CACHE_NAME = 'wpd-v5.3.0';
const urlsToCache = [
  '/index.html',
  '/wpd.min.js',
  '/styles/main.css'
];

self.addEventListener('install', event => {
  event.waitUntil(
    caches.open(CACHE_NAME)
      .then(cache => cache.addAll(urlsToCache))
  );
});

self.addEventListener('fetch', event => {
  event.respondWith(
    caches.match(event.request)
      .then(response => response || fetch(event.request))
  );
});
```

3. Добавьте в HTML:
```html
<link rel="manifest" href="/manifest.json">
<script>
  if ('serviceWorker' in navigator) {
    navigator.serviceWorker.register('/sw.js');
  }
</script>
```

Пользователи Android смогут установить через Chrome: Menu → "Add to Home screen"

---

## ❌ Методы, которые НЕ работают без интернета

### Apache Cordova
```bash
cordova platform add android  # Требует загрузку ~200 MB
cordova build android          # Требует Gradle + Maven
```

### React Native / Expo
Те же проблемы - используют Gradle под капотом.

### Manual APK build
Теоретически возможно собрать APK вручную через `aapt2` + `d8` + `zipalign` + `apksigner`, но:
- Требует Android SDK Build Tools (нужен интернет для загрузки)
- Очень сложный процесс
- Не рекомендуется

---

## 🎯 Рекомендация

**Для вашего случая лучший вариант: Docker (Решение 1)**

1. Требует интернет только при первой сборке образа
2. После этого образ можно сохранить и использовать офлайн
3. Простое использование: `./build-apk-docker.sh`
4. Воспроизводимые сборки

**Если Docker недоступен: GitHub Actions (Решение 2)**
- Полностью автоматическая сборка
- Не требует локальной настройки

**Если APK не критичен: PWA (Решение 5)**
- Не требует сборки
- Работает везде

---

## Сравнение методов

| Метод | Интернет | Сложность | Время сборки | Рекомендация |
|-------|----------|-----------|--------------|--------------|
| Docker | Только первый раз | Низкая | 3-5 мин | ⭐⭐⭐⭐⭐ |
| GitHub Actions | Всегда | Низкая | 5-10 мин | ⭐⭐⭐⭐ |
| Локальная сборка | Первый раз | Средняя | 3-5 мин | ⭐⭐⭐ |
| App Center | Всегда | Средняя | 10-20 мин | ⭐⭐ |
| PWA | Нет | Низкая | 1 мин | ⭐⭐⭐⭐ |

---

## Дополнительная информация

- Все методы используют Gradle - альтернативы не существует
- Android SDK обязателен для любой сборки APK
- PWA - единственный вариант полностью без APK
- Docker - лучший компромисс между удобством и контролем
